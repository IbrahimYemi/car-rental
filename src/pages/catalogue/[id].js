import React from "react";
import styles from "../../styles/Catalogue.module.scss";

const SingleCarPage = () => {
	return (
		<main className={styles.catalogue}>
			<div className={styles.single}>Dead</div>
		</main>
	);
};

export default SingleCarPage;
