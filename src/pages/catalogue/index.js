import React from "react";
import styles from "../../styles/Catalogue.module.scss";
const Catalogue = () => {
	return <main className={styles.catalogue}>catalogue</main>;
};

export default Catalogue;
